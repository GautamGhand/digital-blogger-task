<button type="button" class="btn text-nowrap p-0" data-bs-toggle="popover" data-bs-offset="0,14"
    data-bs-placement="bottom" data-bs-html="true"
    data-bs-content='
                    {{-- @can('updateCashiers')
                        <a class="dropdown-item" href="{{ route('users.edit', $cashier->uuid) }}"><i class="bx bx-edit-alt me-1"></i>
                            Edit</a>
                    @endcan
                    @can('deleteCashiers')
                        <a class="dropdown-item delete_cashier" href="#" data-id="{{ $cashier->uuid }}">
                            <i class="bx bx-trash me-1"></i> Delete
                        </a>
                    @endcan' --}}
    title="">
    <i class="bx bx-dots-vertical-rounded"></i>
</button>
